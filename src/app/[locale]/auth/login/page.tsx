// src/app/[locale]/auth/login/page.tsx
// Page d'authentification adulte — wrappée sans AppLayout (full screen)
import AuthScreen from '@/components/features/auth/AuthScreen';

export default function AuthLoginPage() {
  return <AuthScreen />;
}
